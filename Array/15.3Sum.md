# Code :
class Solution {
    public List<List<Integer>> threeSum(int[] nums) {
        List<List<Integer>> ans = new ArrayList<>();
        for (int i = 0; i < nums.length - 2; i++) {
            for (int j = i + 1; j < nums.length - 1; j++) {
                for (int k = j + 1; k < nums.length; k++) {
                    if (nums[i] + nums[j] + nums[k] == 0) {
                        int a = nums[i];
                        int b = nums[j];
                        int c = nums[k];
                        // Sort a, b, c manually
                        if (a > b) {
                            int temp = a;
                            a = b;
                            b = temp;
                        }
                        if (b > c) {
                            int temp = b;
                            b = c;
                            c = temp;
                        }
                        if (a > b) {
                            int temp = a;
                            a = b;
                            b = temp;
                        }
                        List<Integer> triplet = new ArrayList<>();
                        triplet.add(a);
                        triplet.add(b);
                        triplet.add(c);
                        if (!ans.contains(triplet)) {
                            ans.add(triplet);
                        }
                    }
                }
            }
        }
        return ans;
    }
}



# Explanation :
                 Start
                   │
                   ▼
          Input array nums
                   │
                   ▼
      i = 0 to nums.length - 3
                   │
                   ▼
     j = i + 1 to nums.length - 2
                   │
                   ▼
      k = j + 1 to nums.length - 1
                   │
                   ▼
   nums[i] + nums[j] + nums[k] == 0 ?
             ┌──────────┴──────────┐
             │                     │
            Yes                    No
             │                     │
             ▼                     ▼
  Sort the 3 numbers          Continue
  Check if already exists         │
             │                    │
     ┌───────┴────────┐           │
     │                │           │
   Exists?           Doesn't      │
     │              Exist         │
     ▼                ▼           │
 Skip triplet     Add to Answer   │
     │                │           │
     └────────┬───────┘           │
              ▼                   │
          Next k                  │
              │                   │
              ▼                   │
         Next j when k ends       │
              │                   │
              ▼                   │
         Next i when j ends       │
              │
              ▼
      Return Answer List
              │
              ▼
             End


Time Complexity will be O(n^3) since in code contain() function takes O(m) time so we have 3 loops (i,j,k) so i*j*k makes to O(n^3) time.
Space Complexity will be O(1) since few variables are used
